# Public example assets

`urdf/demo_six_axis.urdf` is an authored, mesh-free six-axis robot used by the
default registry. It keeps the public repository runnable without redistributing
vendor-specific robot models or meshes. Replace it with a licensed URDF when
testing a real robot or a mesh-based scene.
